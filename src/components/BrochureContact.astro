---
interface Props {
  locale?: string;
}

const { locale = "es" } = Astro.props;

const translations = await import(`../locales/${locale}.json`);
const t = translations.default;
---

<section class="brochure-contact" id="contact-section">
  <div class="brochure-contact-container">
    <div class="section-header">
      <h2 class="reveal-text" data-t="brochure.contact.sectionTitle">
        {t.brochure.contact.sectionTitle}
      </h2>
      <div class="header-line"></div>
      <p class="section-intro" data-t="brochure.contact.intro">
        {t.brochure.contact.intro}
      </p>
    </div>

    <div class="contact-options">
      <a
        href="mailto:hola@thirdstudio.tech"
        class="contact-card"
        data-option="email"
      >
        <span class="contact-number">01</span>
        <h3 class="contact-title">Email</h3>
        <p class="contact-info" data-t="brochure.contact.email">
          {t.brochure.contact.email}
        </p>
        <div class="contact-arrow">→</div>
        <div class="contact-bg email-bg"></div>
      </a>

      <a
        href="https://wa.me/+5213338153789"
        target="_blank"
        rel="noopener noreferrer"
        class="contact-card"
        data-option="whatsapp"
      >
        <span class="contact-number">02</span>
        <h3 class="contact-title">WhatsApp</h3>
        <p class="contact-info" data-t="brochure.contact.phone">
          {t.brochure.contact.phone}
        </p>
        <div class="contact-arrow">→</div>
        <div class="contact-bg whatsapp-bg"></div>
      </a>

      <a
        href="https://www.thirdstudio.tech"
        target="_blank"
        rel="noopener noreferrer"
        class="contact-card"
        data-option="website"
      >
        <span class="contact-number">03</span>
        <h3 class="contact-title">Website</h3>
        <p class="contact-info" data-t="brochure.contact.website">
          {t.brochure.contact.website}
        </p>
        <div class="contact-arrow">→</div>
        <div class="contact-bg website-bg"></div>
      </a>

      <button
        type="button"
        class="contact-card"
        id="download-vcard-brochure"
        data-option="download"
      >
        <span class="contact-number">04</span>
        <h3 class="contact-title" data-t="contact.saveContact">
          {t.contact.saveContact}
        </h3>
        <p class="contact-info" data-t="brochure.contact.downloadContact">
          {t.brochure.contact.downloadContact}
        </p>
        <div class="contact-arrow">↓</div>
        <div class="contact-bg download-bg"></div>
      </button>
    </div>
  </div>
</section>

<style>
  .brochure-contact {
    position: relative;
    width: 100%;
    min-height: 80vh;
    background-color: #111111;
    padding: 15vh 5vw;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .brochure-contact-container {
    width: 100%;
    max-width: 1600px;
    margin: 0 auto;
  }

  .section-header {
    margin-bottom: 10vh;
    text-align: left;
  }

  .section-header h2 {
    font-size: clamp(3rem, 8vw, 10rem);
    color: #ffffff;
    font-weight: 800;
    line-height: 0.9;
    text-transform: uppercase;
    margin-bottom: 40px;
    font-family: "Inter", system-ui, sans-serif;
    letter-spacing: -0.04em;
  }

  .header-line {
    width: 0;
    height: 4px;
    background: linear-gradient(90deg, #a855f7 0%, #ec4899 100%);
    margin-bottom: 30px;
  }

  .section-intro {
    font-size: clamp(1.1rem, 1.8vw, 1.4rem);
    color: rgba(255, 255, 255, 0.7);
    max-width: 800px;
    line-height: 1.6;
  }

  .contact-options {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1px;
    background-color: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .contact-card {
    position: relative;
    height: 280px;
    padding: 40px;
    background-color: #0a0a0a;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    cursor: pointer;
    overflow: hidden;
    transition: background-color 0.6s ease;
    text-decoration: none;
    border: none;
    text-align: left;
  }

  .contact-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transform: scaleY(0);
    transform-origin: bottom;
    transition: transform 0.7s cubic-bezier(0.19, 1, 0.22, 1);
    z-index: 1;
  }

  .email-bg {
    background: linear-gradient(135deg, #ec4899 0%, #f43f5e 100%);
  }

  .whatsapp-bg {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  }

  .website-bg {
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
  }

  .download-bg {
    background: linear-gradient(135deg, #a855f7 0%, #9333ea 100%);
  }

  .contact-card:hover .contact-bg {
    transform: scaleY(1);
  }

  .contact-number {
    font-family: "Geist Mono", "Fira Code", monospace;
    font-size: 1rem;
    font-weight: 500;
    position: relative;
    z-index: 2;
    transition: color 0.4s ease;
  }

  .contact-card[data-option="email"] .contact-number {
    color: #ec4899;
  }

  .contact-card[data-option="whatsapp"] .contact-number {
    color: #10b981;
  }

  .contact-card[data-option="website"] .contact-number {
    color: #3b82f6;
  }

  .contact-card[data-option="download"] .contact-number {
    color: #a855f7;
  }

  .contact-card:hover .contact-number {
    color: #ffffff;
  }

  .contact-title {
    font-size: clamp(1.5rem, 2vw, 2.5rem);
    color: #ffffff;
    font-weight: 700;
    text-transform: uppercase;
    margin: 20px 0;
    position: relative;
    z-index: 2;
    transition: color 0.4s ease;
  }

  .contact-info {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.6);
    position: relative;
    z-index: 2;
    transition: color 0.4s ease;
  }

  .contact-arrow {
    position: absolute;
    bottom: 40px;
    right: 40px;
    font-size: 2rem;
    color: rgba(255, 255, 255, 0.3);
    position: relative;
    z-index: 2;
    transition:
      color 0.4s ease,
      transform 0.4s ease;
  }

  .contact-card:hover .contact-arrow {
    color: #ffffff;
    transform: translateX(10px);
  }

  @media (max-width: 900px) {
    .brochure-contact {
      padding: 10vh 5vw;
    }

    .section-header h2 {
      font-size: 2.5rem;
    }

    .contact-options {
      grid-template-columns: 1fr;
    }

    .contact-card {
      height: 220px;
      padding: 30px;
    }
  }
</style>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener("DOMContentLoaded", () => {
    const section = document.querySelector(".brochure-contact");
    if (!section) return;

    const header = section.querySelector(".section-header h2");
    const headerLine = section.querySelector(".header-line");
    const intro = section.querySelector(".section-intro");
    const cards = section.querySelectorAll(".contact-card");

    gsap.set(header, { y: 100, opacity: 0 });
    gsap.set(headerLine, { width: 0 });
    gsap.set(intro, { y: 30, opacity: 0 });
    gsap.set(cards, { y: 80, opacity: 0 });

    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: section,
        start: "top 80%",
        toggleActions: "play none none reverse",
      },
    });

    tl.to(header, {
      y: 0,
      opacity: 1,
      duration: 1.2,
      ease: "expo.out",
    })
      .to(
        headerLine,
        {
          width: "200px",
          duration: 1.5,
          ease: "power4.inOut",
        },
        "-=0.8",
      )
      .to(
        intro,
        {
          y: 0,
          opacity: 1,
          duration: 0.8,
          ease: "power2.out",
        },
        "-=1",
      )
      .to(
        cards,
        {
          y: 0,
          opacity: 1,
          duration: 0.8,
          stagger: 0.15,
          ease: "power3.out",
        },
        "-=0.5",
      );

    // vCard download functionality
    const downloadBtn = document.getElementById("download-vcard-brochure");
    if (downloadBtn) {
      downloadBtn.addEventListener("click", () => {
        const vCardData = `BEGIN:VCARD
VERSION:3.0
FN:Fernando Gonzalez Davalos
N:Gonzalez Davalos;Fernando;;;
ORG:THIRD Dev Studio
TEL;TYPE=CELL:+5213338153789
EMAIL:hola@thirdstudio.tech
END:VCARD`;

        const blob = new Blob([vCardData], { type: "text/vcard" });
        const url = window.URL.createObjectURL(blob);
        const link = document.createElement("a");
        link.href = url;
        link.download = "Fernando_Gonzalez_Davalos_Contact.vcf";
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
        window.URL.revokeObjectURL(url);
      });
    }
  });
</script>
